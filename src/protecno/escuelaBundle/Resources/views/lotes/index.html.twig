{% extends '::base.html.twig' %}

{% block body -%}
    <h1>lotes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Fechadeincio</th>
                <th>Fechadefinal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lotes_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>{% if entity.fechaDeIncio %}{{ entity.fechaDeIncio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaDeFinal %}{{ entity.fechaDeFinal|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lotes_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lotes_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lotes_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
