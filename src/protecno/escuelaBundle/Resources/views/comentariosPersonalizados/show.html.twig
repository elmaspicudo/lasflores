{% extends '::base.html.twig' %}

{% block body -%}
    <h1>comentariosPersonalizados</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tema</th>
                <td>{{ entity.tema }}</td>
            </tr>
            <tr>
                <th>Obserbacionpor</th>
                <td>{{ entity.obserbacionPor }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('comentariospersonalizados') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('comentariospersonalizados_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
