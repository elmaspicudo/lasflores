{% extends '::base.html.twig' %}

{% block body -%}
    <h1>carpetaEspecificaDelUsuario</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombredelacarpeta</th>
                <td>{{ entity.nombreDeLaCarpeta }}</td>
            </tr>
            <tr>
                <th>Estudiante</th>
                <td>{{ entity.estudiante }}</td>
            </tr>
            <tr>
                <th>Empleado</th>
                <td>{{ entity.empleado }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ entity.activo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('carpetaespecificadelusuario') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('carpetaespecificadelusuario_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
