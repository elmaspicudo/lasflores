{% extends '::base.html.twig' %}

{% block body -%}
    <h1>carpetaEspecificaDelUsuario list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombredelacarpeta</th>
                <th>Estudiante</th>
                <th>Empleado</th>
                <th>Activo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('carpetaespecificadelusuario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreDeLaCarpeta }}</td>
                <td>{{ entity.estudiante }}</td>
                <td>{{ entity.empleado }}</td>
                <td>{{ entity.activo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('carpetaespecificadelusuario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('carpetaespecificadelusuario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('carpetaespecificadelusuario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
