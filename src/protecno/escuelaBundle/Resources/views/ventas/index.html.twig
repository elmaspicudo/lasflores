{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ventas list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantidad</th>
                <th>Precioporunidad</th>
                <th>Preciototal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ventas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cantidad }}</td>
                <td>{{ entity.precioPorUnidad }}</td>
                <td>{{ entity.precioTotal }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ventas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ventas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ventas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
