{% extends '::base.html.twig' %}

{% block body -%}
    <h1>nuevoCliente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombredelcliente</th>
                <th>Redirigidaurl</th>
                <th>Verificado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('nuevocliente_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreDelCliente }}</td>
                <td>{{ entity.redirigidaURL }}</td>
                <td>{{ entity.verificado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('nuevocliente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('nuevocliente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('nuevocliente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
