{% extends '::base.html.twig' %}

{% block body -%}
    <h1>nuevoCliente</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombredelcliente</th>
                <td>{{ entity.nombreDelCliente }}</td>
            </tr>
            <tr>
                <th>Redirigidaurl</th>
                <td>{{ entity.redirigidaURL }}</td>
            </tr>
            <tr>
                <th>Verificado</th>
                <td>{{ entity.verificado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('nuevocliente') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('nuevocliente_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
