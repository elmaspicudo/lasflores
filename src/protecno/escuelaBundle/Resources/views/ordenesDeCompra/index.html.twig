{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ordenesDeCompra list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ordendecomprano</th>
                <th>Fecha</th>
                <th>Referencia</th>
                <th>Preciounitario</th>
                <th>Cantidad</th>
                <th>Descuento</th>
                <th>Impuestos</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ordenesdecompra_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ordenDeCompraNo }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.referencia }}</td>
                <td>{{ entity.precioUnitario }}</td>
                <td>{{ entity.cantidad }}</td>
                <td>{{ entity.descuento }}</td>
                <td>{{ entity.impuestos }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ordenesdecompra_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ordenesdecompra_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ordenesdecompra_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
