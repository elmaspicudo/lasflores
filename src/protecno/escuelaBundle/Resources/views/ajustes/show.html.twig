{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ajustes</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Identificacionbiometrica</th>
                <td>{{ entity.identificacionBiometrica }}</td>
            </tr>
            <tr>
                <th>Habilitarfuncionesdecorreoelectronico</th>
                <td>{{ entity.habilitarFuncionesDeCorreoElectronico }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ajustes') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ajustes_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
