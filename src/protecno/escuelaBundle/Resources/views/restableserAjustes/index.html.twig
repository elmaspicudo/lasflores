{% extends '::base.html.twig' %}

{% block body -%}
    <h1>restableserAjustes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Habilitarrestablesimientoautomatico</th>
                <th>Dejarestableserperiodomes</th>
                <th>Fiablesfechaempezar</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('restableserajustes_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.habilitarRestablesimientoAutomatico }}</td>
                <td>{{ entity.dejaRestableserPeriodoMes }}</td>
                <td>{% if entity.fiablesFechaEmpezar %}{{ entity.fiablesFechaEmpezar|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('restableserajustes_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('restableserajustes_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('restableserajustes_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
