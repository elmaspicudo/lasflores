{% extends '::base.html.twig' %}

{% block body -%}
    <h1>tiempoDeClase list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Horadeinicio</th>
                <th>Horadefinalizacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tiempodeclase_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{% if entity.horaDeInicio %}{{ entity.horaDeInicio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.horaDeFinalizacion %}{{ entity.horaDeFinalizacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tiempodeclase_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tiempodeclase_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tiempodeclase_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
