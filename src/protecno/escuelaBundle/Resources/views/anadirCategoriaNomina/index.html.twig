{% extends '::base.html.twig' %}

{% block body -%}
    <h1>anadirCategoriaNomina list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombredelacategoria</th>
                <th>Porcentaje</th>
                <th>Porcentajede</th>
                <th>Estado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('anadircategorianomina_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreDeLaCategoria }}</td>
                <td>{{ entity.porcentaje }}</td>
                <td>{{ entity.porcentajeDe }}</td>
                <td>{{ entity.estado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('anadircategorianomina_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('anadircategorianomina_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('anadircategorianomina_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
