{% extends '::base.html.twig' %}

{% block body -%}
    <h1>anadirCategoriaNomina</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombredelacategoria</th>
                <td>{{ entity.nombreDeLaCategoria }}</td>
            </tr>
            <tr>
                <th>Porcentaje</th>
                <td>{{ entity.porcentaje }}</td>
            </tr>
            <tr>
                <th>Porcentajede</th>
                <td>{{ entity.porcentajeDe }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.estado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('anadircategorianomina') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('anadircategorianomina_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
