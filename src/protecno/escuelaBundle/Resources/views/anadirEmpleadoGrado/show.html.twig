{% extends '::base.html.twig' %}

{% block body -%}
    <h1>anadirEmpleadoGrado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Prioridad</th>
                <td>{{ entity.prioridad }}</td>
            </tr>
            <tr>
                <th>Numeromaximodehoraspordia</th>
                <td>{{ entity.numeroMaximoDeHorasPorDia }}</td>
            </tr>
            <tr>
                <th>Numeromaximodehorasporsemana</th>
                <td>{{ entity.numeroMaximoDeHorasPorSemana }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.estado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('anadirempleadogrado') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('anadirempleadogrado_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
