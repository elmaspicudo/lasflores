{% extends '::base.html.twig' %}

{% block body -%}
    <h1>gasto list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Seleccionarfechadeinicio</th>
                <th>Seleccionarfechadefinalizacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gasto_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.seleccionarFechaDeInicio %}{{ entity.seleccionarFechaDeInicio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.seleccionarFechaDeFinalizacion %}{{ entity.seleccionarFechaDeFinalizacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gasto_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gasto_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gasto_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
