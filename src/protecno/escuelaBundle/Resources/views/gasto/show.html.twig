{% extends '::base.html.twig' %}

{% block body -%}
    <h1>gasto</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Seleccionarfechadeinicio</th>
                <td>{{ entity.seleccionarFechaDeInicio|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Seleccionarfechadefinalizacion</th>
                <td>{{ entity.seleccionarFechaDeFinalizacion|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('gasto') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('gasto_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
