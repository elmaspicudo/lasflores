{% extends '::base.html.twig' %}

{% block body -%}
    <h1>proveedores list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombredelproveedor</th>
                <th>Nocontacto</th>
                <th>Direccion</th>
                <th>Notin</th>
                <th>Region</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('proveedores_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreDelProveedor }}</td>
                <td>{{ entity.noContacto }}</td>
                <td>{{ entity.direccion }}</td>
                <td>{{ entity.noTin }}</td>
                <td>{{ entity.region }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('proveedores_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('proveedores_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('proveedores_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
