{% extends '::base.html.twig' %}

{% block body -%}
    <h1>cuotaColeccion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cuotanombredelacoleccion</th>
                <th>Fechadeinicio</th>
                <th>Fechafinal</th>
                <th>Fechadevencimiento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cuotacoleccion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cuotaNombreDeLaColeccion }}</td>
                <td>{% if entity.fechaDeInicio %}{{ entity.fechaDeInicio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaFinal %}{{ entity.fechaFinal|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaDeVencimiento %}{{ entity.fechaDeVencimiento|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cuotacoleccion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cuotacoleccion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cuotacoleccion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
